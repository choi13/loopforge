# Copy this file to `.env` (which is gitignored) and paste your key.
# The server auto-loads .env at startup; the live "anthropic" provider needs this.
ANTHROPIC_API_KEY=sk-ant-...

# Private external dashboard deployment.
LOOPFORGE_ALLOWED_WEB_ORIGINS=https://loop.sbchoi-api.kr
LOOPFORGE_COMMAND_EXECUTOR=docker
